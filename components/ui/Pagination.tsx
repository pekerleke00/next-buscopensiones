import React from 'react'

interface Props {
    totalAmount: number
}

export const Pagination = (props: Props) => {
    const { totalAmount } = props;

    return (
        <div>
            
        </div>
    )
}
